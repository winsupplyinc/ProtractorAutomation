import * as L from "./index";
var listStyle = {
    style: "list-style-type: none; padding: 0; margin: 0 0 0 22px; font-style: normal"
};
var gw = typeof window === undefined ? global : window;
if (gw.devtoolsFormatters === undefined) {
    gw.devtoolsFormatters = [];
}
function createReference(object) {
    return ["object", { object: object }];
}
var l = /*#__PURE__*/L.list("hello", "world");
L.intersperse(", ", l);
gw.devtoolsFormatters.push({
    header: function (l) {
        if (L.isList(l)) {
            return ["div", { style: "font-style: italic" }, ["span", {}, "(" + l.length + ") list("]].concat(L.toArray(L.intersperse(", ", L.map(createReference, l))), [["span", {}, ")"]]);
        } else {
            return null;
        }
    },
    hasBody: function (l) {
        return l.length !== 0;
    },
    body: function (l) {
        var idx = 0;
        var children = L.map(function (o) {
            return ["li", ["span", {}, idx++ + ": "], createReference(o)];
        }, l);
        return ["ol", listStyle].concat(L.toArray(children));
    }
});